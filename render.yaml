services:
  - type: web
    name: clickalinks-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: NODE_ENV
        value: production
      - key: STRIPE_SECRET_KEY
        fromSecret: true